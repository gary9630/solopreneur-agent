# Workspace Agent Notes

This workspace is for the deal-to-delivery NemoClaw demo agent.

The agent may draft records, create engineering issues, and send status updates through the approved tool gateway. It must keep a durable audit trail for high-impact actions and must never store secrets in workspace files.

Do not finalize invoices or mark deals won unless the operator provides explicit acceptance.
